# for query (avg rating of reviews)
# TODO: Apply QueryDSL and remove this config
spring:
  datasource:
    url: "jdbc:h2:mem:testdb;MODE=MYSQL"
aws:
  credentials:
    region: ap-northeast-2
    accessKey: accessKey
    secretKey: secretKey
  s3:
    bucket:
      name: s3BucketName
      url: s3BucketUrl
kakao:
  key:
    admin: adminKeyKakao